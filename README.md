git add README.md .gitignore
git commit -m "Initial commit: add README and gitignore"

git add expense.py
git commit -m "Add Expense class (OOP model for a single record)"

git add storage.py
git commit -m "Add CSV-based persistent storage module"

git add operations.py
git commit -m "Implement add, view, and delete expense operations"

git add operations.py
git commit -m "Add search, filter, and update functionality"

git add summary.py
git commit -m "Add category-wise and monthly spending summaries"

git add utils.py
git commit -m "Add input validation and custom exception handling"

git add main.py
git commit -m "Add CLI menu and wire up all modules in main.py"

git add docs/
git commit -m "Add internship report to docs folder"

git add README.md
git commit -m "Update README with usage instructions and examples"
